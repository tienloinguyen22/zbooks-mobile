import { StyleSheet } from 'react-native';
import { colors } from '@app/core';

export const styles = StyleSheet.create({
  default: {
    fontSize: 13,
    color: colors.red,
  },
  formError: {
    marginLeft: 15,
    marginTop: 5,
    marginBottom: 5,
  },
});
