import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  formItemContainer: {
    marginBottom: 16,
    width: '100%',
  },
  labelContainer: {
    paddingHorizontal: 4,
    marginBottom: 4,
  },
  errorContainer: {
    marginTop: 2,
    paddingHorizontal: 12,
  },
});
