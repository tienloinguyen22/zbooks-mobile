import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  formItemContainer: {
    marginBottom: 16,
  },
  errorContainer: {
    marginTop: 2,
    paddingHorizontal: 12,
  },
});
