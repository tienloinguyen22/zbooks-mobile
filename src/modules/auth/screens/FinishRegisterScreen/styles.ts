import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titleContainer: {
    marginBottom: 12,
  },
  welcomeContainer: {
    marginTop: 120,
    marginBottom: 84,
  },
  textCenter: {
    textAlign: 'center',
    paddingHorizontal: 12,
  },
  formContainer: {
    alignItems: 'center',
  },
  input: {
    width: 300,
  },
});
