import { StyleSheet } from 'react-native';
import { colors } from '@app/core';

export const styles = StyleSheet.create({
  loadingPadding: {
    paddingVertical: 12,
  },
  greyText: {
    color: colors.grey,
  },
});
