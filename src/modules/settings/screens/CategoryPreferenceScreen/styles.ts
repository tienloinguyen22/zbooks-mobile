import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titleContainer: {
    paddingVertical: 20,
    paddingBottom: 32,
  },
  categoryList: {
    flex: 1,
    paddingBottom: 24,
  },
});
